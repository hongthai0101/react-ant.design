export interface ICapitalFlowType {
    createdAt: string
    updatedAt: string
    id: string
    uid: number
    type: number
    name: string
    sortIndex: number

    // append
    typeName: string
    color: string 
}