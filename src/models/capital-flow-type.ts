import { ListRequest } from "./common"

export interface ICapitalFlowType {
    createdAt: string
    updatedAt: string
    id: string
    uid: number
    type: string
    name: string
    sortIndex: number
}